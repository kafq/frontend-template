import { css } from 'emotion';

const styles = {
  container: css({
    padding: '1rem 0.5rem',
    margin: '1rem',
    minHeight: '300px',
    minWidth: '180px',
    border: '1px solid #cdcdcd',
    display: 'inline-block',  
  })
}

export { styles }